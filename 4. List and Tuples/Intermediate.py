#1. List Manipulation
'''
    Remove all occurrences of a given element from a list.
    Replace every second element in a list with 0.
    Rotate a list [1, 2, 3, 4, 5] to the left by 2 positions → Output: [3, 4, 5, 1, 2].
    Split a list into two halves and swap them.
    Find and remove duplicate elements from a list.
'''
    
#2. Sorting & Searching in Lists
'''
    Find the second largest element in a list.
    Sort a list of numbers without using the built-in sort() function.
    Implement binary search to find an element in a sorted list.
    Given a list of tuples representing student names and scores, sort them by score.

    students = [('Alice', 85), ('Bob', 72), ('Charlie', 90)]

    Find the most frequent element in a list.
'''
    

#3. List Comprehensions & Advanced Operations
'''
    Generate a list of squares of numbers from 1 to 20 using list comprehension.
    Extract all vowels from a given string and store them in a list.
    Convert a list of integers to a list of their string equivalents.
    Flatten a nested list [[1, 2], [3, 4], [5, 6]] into [1, 2, 3, 4, 5, 6].
    Remove all even numbers from a list using list comprehension.
'''
    
#4. Mathematical and Statistical Operations
'''
    Compute the mean, median, and mode of a list of numbers.
    Find the product of all numbers in a list without using math.prod().
    Calculate the cumulative sum of elements in a list.
    Given two lists, create a third list that contains only the common elements.
    Find all pairs in a list whose sum is equal to a given number.
'''


#Tuples#

#1. Tuple Packing & Unpacking
'''
    Create a tuple with five elements and unpack them into five variables.
    Swap two numbers using tuple unpacking.
'''


#2. Tuple Functions & Methods
'''
    Find the maximum and minimum values in a tuple (10, 25, 7, 98, 34).
    Count the occurrences of the number 3 in the tuple (3, 5, 3, 8, 3, 10).
    Find the index of the first occurrence of 20 in the tuple (5, 10, 15, 20, 25, 20, 30).
    Find the sum of all numbers in the tuple (4, 8, 12, 16, 20).
'''
    

#3. Tuple Manipulation & Conversions
'''
    Convert a list [1, 2, 3, 4, 5] into a tuple.
    Merge two tuples (1, 2, 3) and (4, 5, 6) into a single tuple.
    Extract a sub-tuple (slicing) from (10, 20, 30, 40, 50, 60, 70, 80) to get only (30, 40, 50).
    Convert a tuple of numbers (100, 200, 300) into a list and modify the second element to 250.
'''
    

#4. Tuple-Based Problem Solving
'''
    Write a program to find the most frequent element in the tuple (4, 7, 2, 7, 4, 9, 7, 4).
    Sort the tuple (5, 2, 8, 1, 9, 3) in ascending order.
    Given a tuple ((1, 3), (2, 2), (4, 1), (3, 5)), sort it based on the second element of each tuple.
    Remove duplicate elements from the tuple (5, 10, 5, 20, 10, 30).
    Check if two tuples (1, 2, 3) and (1, 2, 3, 4) are identical in content.
'''


#5. Using Tuples with Loops & Conditions
'''
    Write a program to iterate through a tuple and print all even numbers.
    Create a tuple of names and check if a specific name exists in it.
    Generate a tuple with squares of numbers from 1 to 10 using a loop.
    Given a tuple ((2, 'A'), (1, 'B'), (3, 'C')), print the values in sorted order based on the first number.
    Find the intersection (common elements) of two tuples (1, 2, 3, 4, 5) and (3, 4, 5, 6, 7).
'''